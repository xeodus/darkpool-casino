services:
  pg:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ricky
      POSTGRES_PASSWORD: Lucifer
      POSTGRES_DB: goquant
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ricky -d goquant"]
      interval: 2s
      timeout: 2s
      retries: 20
volumes:
  pgdata:
